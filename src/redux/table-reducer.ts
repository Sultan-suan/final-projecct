
type StateType = {

}

const initialState = {

}

type ActionType = {
    type: ''
}

export const tableReducer = (state: StateType = initialState, action: ActionType) => {
    switch (action.type) {
        case '':
            return {}
        case '':
            return {}
        default:
            return state;
    }
}